<!DOCTYPE html>
<html lang="en" class="height-full" data-color-mode="auto" data-light-theme="light" data-dark-theme="dark">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Setup RunsOn</title>
    <link rel="icon" href="/static/favicon.png">
    <link rel="stylesheet" href="/static/primer.css">
    <style>
      body { font-size: 16px; }
      .btn-big { font-size: 150%; padding: 14px 32px; }
    </style>
  </head>
  <body class="height-full bg-gray-light">
    <div class="d-flex flex-column flex-justify-center flex-items-center text-center height-full">
      <img src="/static/logo.png" alt="RunsOn Logo" width="200" class="mb-6">
      <div class="box-shadow rounded-2 border p-6 bg-white" style="max-width: min(50%,800px)">
        <h1>
          Welcome to RunsOn
        </h1>

        <div class="text-left mt-6">
          <h2 class="alt-h3 mb-2">Getting Started</h2>

          <p>You are a just few clicks away from fast, secure, self-hosted GitHub runners at the best price possible.</p>

          <p>Please click the button below and follow the instructions to install the RunsOn GitHub App in your organization.</p>
          <br>

          <form action="{{ createAppUrl }}" method="post" target="_blank" class="d-flex flex-items-center">
            <button class="btn btn-outline btn-big" name="manifest" id="manifest" value='{{ manifest }}'>Register GitHub App</button>
          </form>

          <br>
          <p>Note: RunsOn is not affiliated to GitHub in any way.</p>
        </div>
      </div>

      <div class="mt-4">
        <h4 class="alt-h4 text-gray-light">Need help?</h4>
        <div class="d-flex flex-justify-center mt-2" style="align-items:center;gap: 1rem;">
          <a href="https://runs-on.com/docs" class="btn btn-outline">Documentation</a>
          <img alt="version check" src="{{ versionCheckUrl }}/{{ pkg.version }}.svg?org={{ pkg.orgName }}">
          <div style="font-size: 75%">{{ license.status }}</div>
        </div>
      </div>
    </div>
  </body>
</html>
